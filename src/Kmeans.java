import java.util.*;
import java.lang.Math;



public class Kmeans {

    usersSet us;
    HashMap<Integer, User> users;

    /*public void runKmeans() {
        us = usersSet.getInstance();
        users = us.getUsersSet();
        ArrayList<Cluster> clusters = createCentroids();
    }
    private ArrayList<Cluster> createCentroids(int K) {
        ArrayList<Integer> clusterSuitors = new ArrayList<>();
        for(int i = 0; i < K*10; ++i) {
            Random r = new Random();
            int randomIndex = r.nextInt(users.size());
            //if(!)
        }
    }*/

}